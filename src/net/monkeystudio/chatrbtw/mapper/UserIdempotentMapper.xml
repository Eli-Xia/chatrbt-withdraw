<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" > 
<mapper namespace="net.monkeystudio.chatrbtw.mapper.UserIdempotentMapper">
    <resultMap id="baseResultMap" type="net.monkeystudio.chatrbtw.entity.UserIdempotent">
        <result column="id" property="id" jdbcType="INTEGER" />
        <result column="user_id" property="userid" jdbcType="INTEGER" />
        <result column="state" property="state" jdbcType="INTEGER" />
    </resultMap>
    <sql id="Base_Column_List" >
        `id`, `state`, `create_time`
    </sql>

    <update id="updateState" >
        update e_user_idempotent set
            state = #{state}
                     WHERE id = #{id}
    </update>

    <insert id="insert" parameterType="com.wecan.starworld.model.UserIdempotent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO
            e_user_idempotent (`state`, `user_id`)
        SELECT
            #{state} , #{userId}
        FROM DUAL WHERE NOT EXISTS  (
            SELECT
                `state`, `user_id`
            FROM
                e_user_idempotent
            WHERE
                `state` = #{state}
            AND
                `user_id` = #{userId}
        )
    </insert>


</mapper>
