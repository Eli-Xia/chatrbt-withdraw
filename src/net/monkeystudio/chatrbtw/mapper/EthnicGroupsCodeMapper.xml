<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" > 
<mapper namespace="net.monkeystudio.chatrbtw.mapper.EthnicGroupsCodeMapper">
    <resultMap id="baseResultMap" type="net.monkeystudio.chatrbtw.entity.EthnicGroupsCode">
        <result column="id" property="id" jdbcType="INTEGER" />
        <result column="code" property="code" jdbcType="VARCHAR" />
        <result column="parent_code" property="parentCode" jdbcType="VARCHAR" />
        <result column="total_valid_count" property="totalValidCount" jdbcType="INTEGER" />
        <result column="total_valid_count" property="totalValidCount" jdbcType="INTEGER" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, code, parent_code, total_valid_count ,wx_pub_origin_id
    </sql>

    <select id="selectByWxPubAndCode" parameterType="map">
        select
            <include refid="Base_Column_List"></include>
        from
            e_ethnic_groups_code
        where
            wx_pub_origin_id = #{wxPubOrginId} and `code` = #{code}
    </select>


    <select id="countByCode" parameterType="map" resultType="java.lang.Integer">
        select
            count(id)
        from
            e_ethnic_groups_code
        where
            `code` = #{code}
        and
            parent_code is not null
    </select>

</mapper>
