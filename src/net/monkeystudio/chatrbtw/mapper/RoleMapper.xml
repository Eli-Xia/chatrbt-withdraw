<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="net.monkeystudio.chatrbtw.mapper.RoleMapper" >
  <resultMap id="BaseResultMap" type="net.monkeystudio.chatrbtw.entity.Role" >
    <id column="code" property="code" jdbcType="VARCHAR" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="description" property="description" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="insert" parameterType="net.monkeystudio.chatrbtw.entity.Role" >
    insert into e_role (code, name, description
      )
    values (#{code,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR}, #{description,jdbcType=VARCHAR}
      )
  </insert>
  <insert id="insertSelective" parameterType="net.monkeystudio.chatrbtw.entity.Role" >
    insert into e_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="code != null" >
        code,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="description != null" >
        description,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="code != null" >
        #{code,jdbcType=VARCHAR},
      </if>
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="description != null" >
        #{description,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

  <select id="selectByUserName" resultMap="BaseResultMap">
    SELECT r.* FROM e_user u
    JOIN r_user_role u_r ON (u.id = u_r.user_id)
    JOIN e_role r ON (u_r.role = r.code)
    WHERE u.username = #{username}
  </select>

  <select id="selectAll" resultMap="BaseResultMap">
    SELECT  * FROM  e_role
  </select>

  <delete id="delete" parameterType="map">
    delete from e_role where code = #{code}
  </delete>

  <update id="update" >
    update e_role
    set
    name = #{role.name,jdbcType=VARCHAR},
    code = #{role.code,jdbcType=VARCHAR},
    description = #{role.description,jdbcType=VARCHAR}
    where
    code = #{oriCode,jdbcType=VARCHAR}
  </update>
</mapper>